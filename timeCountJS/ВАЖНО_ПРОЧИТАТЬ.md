# ⚠️ ВАЖНО: Как правильно запустить приложение

## Проблема с портом 5501 (Live Server)

Если вы видите ошибку:
```
POST http://127.0.0.1:5501/timeCountJS/api/save_session.php 405 (Method Not Allowed)
```

Это означает, что вы открыли приложение через **Live Server** (порт 5501), который **НЕ поддерживает PHP**.

## ✅ Правильный способ запуска

### Вариант 1: Через XAMPP (Рекомендуется)

1. **Убедитесь, что XAMPP запущен:**
   - Apache должен быть запущен (зеленая лампочка)
   - MySQL должен быть запущен (зеленая лампочка)

2. **Откройте приложение через XAMPP:**
   ```
   http://localhost/timeCountJS/timeCountJS/index1.html
   ```
   
   ИЛИ
   
   ```
   http://127.0.0.1/timeCountJS/timeCountJS/index1.html
   ```

3. **НЕ используйте Live Server!** Live Server работает только для статических файлов (HTML, CSS, JS), но не для PHP.

### Вариант 2: Через встроенный сервер PHP (если установлен PHP)

Если у вас установлен PHP отдельно (не через XAMPP), вы можете использовать встроенный сервер:

```bash
cd C:\xampp\htdocs\timeCountJS\timeCountJS
php -S localhost:8000
```

Затем откройте:
```
http://localhost:8000/index1.html
```

## 🔍 Как проверить, что все работает

1. **Откройте диагностический скрипт:**
   ```
   http://localhost/timeCountJS/timeCountJS/api/test_connection.php
   ```
   
   Должен показать статус подключения к базе данных.

2. **Проверьте консоль браузера (F12):**
   - Не должно быть ошибок 405 (Method Not Allowed)
   - Запросы должны идти на `localhost` или `127.0.0.1`, НЕ на порт 5501

3. **Попробуйте запустить активность:**
   - Кликните на активность
   - Остановите её
   - В консоли должно быть сообщение об успешном сохранении

## ❌ Что НЕ работает

- ❌ Live Server (порт 5501) - не поддерживает PHP
- ❌ Открытие файла напрямую через `file://` - не работает с fetch API
- ❌ Другие статические серверы без поддержки PHP

## ✅ Что работает

- ✅ XAMPP (Apache + MySQL)
- ✅ Встроенный PHP сервер
- ✅ Любой другой веб-сервер с поддержкой PHP (Nginx + PHP-FPM, IIS + PHP и т.д.)

## 🛠️ Если проблемы остаются

1. Проверьте, что Apache запущен в XAMPP
2. Проверьте, что MySQL запущен в XAMPP
3. Откройте `http://localhost/timeCountJS/timeCountJS/api/test_connection.php` для диагностики
4. Проверьте консоль браузера на наличие других ошибок

